import 'package:flutter/material.dart';

class AppColors {
  // Primary Palette - User Specified
  static const Color primary = Color(0xFF3A86FF); // Electric Blue (Primary Action / Energy)
  static const Color primaryDark = Color(0xFF1E60D5);
  static const Color primaryLight = Color(0xFFEBF2FF); // Soft blue tint
  static const Color primaryContainer = Color(0xFFD6E6FF);

  static const Color secondary = Color(0xFFBDB2FF); // Periwinkle / Soft Purple (Secondary Accent)
  static const Color secondaryDark = Color(0xFF8E7BEF);
  static const Color secondaryLight = Color(0xFFF4F2FF);

  static const Color accentPink = Color(0xFFFFD6E0); // Soft Pastel Pink (Highlight / Badge / Pill)
  static const Color accentPinkDark = Color(0xFFE58FA5);
  static const Color accentPinkLight = Color(0xFFFFF0F4);

  static const Color background = Color(0xFFFFF4F4); // Warm Porcelain (App Background Canvas)
  static const Color surface = Colors.white;
  static const Color surfaceCard = Colors.white;
  static const Color surfaceVariant = Color(0xFFFFF0F2);

  // High-End Flat Neutral Tones
  static const Color textPrimary = Color(0xFF1E202A); // Deep Slate Navy
  static const Color textSecondary = Color(0xFF6B7280); // Cool Slate Gray
  static const Color textMuted = Color(0xFF9CA3AF);
  static const Color border = Color(0xFFEDE8E9);
  static const Color borderFocused = Color(0xFF3A86FF);

  // Status Colors (Vibrant Flat Standard)
  static const Color success = Color(0xFF10B981); // Emerald
  static const Color successBg = Color(0xFFD1FAE5);
  static const Color warning = Color(0xFFF59E0B); // Amber
  static const Color warningBg = Color(0xFFFEF3C7);
  static const Color error = Color(0xFFEF4444); // Coral Red
  static const Color errorBg = Color(0xFFFEE2E2);

  // Category Badges
  static const Color productBadge = Color(0xFF3A86FF);
  static const Color serviceBadge = Color(0xFF8E7BEF);
}
